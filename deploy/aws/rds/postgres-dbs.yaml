AWSTemplateFormatVersion: '2010-09-09'
Metadata:
  License: Apache-2.0
Description: 'Creates a Postgres RDS instance'
Parameters:
  DBName:
    Description: The database name
    Type: String
    MinLength: '1'
    MaxLength: '64'
    AllowedPattern: '[a-zA-Z][a-zA-Z0-9]*'
    ConstraintDescription: must begin with a letter and contain only alphanumeric
      characters.
  DBUser:
    Default: 'postgres'
    NoEcho: 'true'
    Description: The database admin account username
    Type: String
    MinLength: '1'
    MaxLength: '16'
    AllowedPattern: '[a-zA-Z][a-zA-Z0-9]*'
    ConstraintDescription: must begin with a letter and contain only alphanumeric characters.
  DBPassword:
    NoEcho: 'true'
    Description: The database admin account password
    Type: String
    MinLength: '1'
    MaxLength: '41'
    AllowedPattern: '[a-zA-Z0-9]+'
    ConstraintDescription: must contain only alphanumeric characters.
  DBAllocatedStorage:
    Default: '5'
    Description: The size of the database (Gb)
    Type: Number
    MinValue: '5'
    MaxValue: '1024'
    ConstraintDescription: must be between 5 and 1024Gb.
  DBInstanceClass:
    Description: The database instance type
    Type: String
    Default: db.t3.small
    ConstraintDescription: must select a valid database instance type for a given region.
  Env:
    Default: prod
    Description: Environment name
    Type: String
    AllowedPattern: '[a-zA-Z0-9]+'
    ConstraintDescription: must be a human readable name like "prod" or "staging".
Resources:
  DatabaseA:
    Type: AWS::RDS::DBInstance
    Properties:
      DBName: !Ref 'DBName'
      DBInstanceIdentifier: {'Fn::ImportValue': !Sub '${DBName}-${Env}-a'}
      AllocatedStorage: !Ref 'DBAllocatedStorage'
      DBInstanceClass: !Ref 'DBInstanceClass'
      Engine: postgres
      MasterUsername: !Ref 'DBUser'
      MasterUserPassword: !Ref 'DBPassword'
    DeletionPolicy: Snapshot

  DatabaseB:
    Type: AWS::RDS::DBInstance
    Properties:
      DBName: !Ref 'DBName'
      DBInstanceIdentifier: { 'Fn::ImportValue': !Sub '${DBName}-${Env}-b' }
      AllocatedStorage: !Ref 'DBAllocatedStorage'
      DBInstanceClass: !Ref 'DBInstanceClass'
      Engine: postgres
      MasterUsername: !Ref 'DBUser'
      MasterUserPassword: !Ref 'DBPassword'
    DeletionPolicy: Snapshot

Outputs:
  DatabaseAJDBCConnectionString:
    Description: JDBC connection string for the master database
    Value: !Join ['', ['jdbc:postgresql://', !GetAtt [DatabaseA, Endpoint.Address], ':',
                       !GetAtt [DatabaseA, Endpoint.Port], /, !Ref 'DBName']]
  DatabaseBJDBCConnectionString:
    Description: JDBC connection string for the master database
    Value: !Join ['', ['jdbc:postgresql://', !GetAtt [DatabaseB, Endpoint.Address], ':',
                       !GetAtt [DatabaseB, Endpoint.Port], /, !Ref 'DBName']]
